// function confirmBan(studentId) {
//   if (confirm("Are you sure you want to ban this user?")) {
//     // Perform the ban action
//     banUser(studentId);
//   }
// }

// function confirmUnban(studentId) {
//   if (confirm("Are you sure you want to unban this user?")) {
//     // Perform the unban action
//     unbanUser(studentId);
//   }
// }

// function banUser(studentId) {
//   window.location.href = "/ban?student_id=" + studentId;
// }

// function unbanUser(studentId) {
//   window.location.href = "/unban?student_id=" + studentId;
// }
